type: array
items:
  allOf:
    - $ref: '../schemas/item.yml'
    - type: object
      properties:
        descriptiveStatistics:
          $ref: '../schemas/descriptive-statistics.yml'
        comparison:
          $ref: '../schemas/comparison.yml'
