type: array
items:
  allOf:
    - $ref: '../schemas/value-group.yml'
    - type: object
      required:
        - aggregations
      properties:
        aggregations:
          type: array
          description: Einzelne Aggregationen
          items:
            allOf:
              - $ref: '../schemas/aggregation.yml'
              - $ref: '../schemas/descriptive-statistics.yml'
