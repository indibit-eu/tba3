description: Lösungshäufigkeiten der Items im Kontext
content:
  'application/json':
    schema:
      type: array
      items:
        allOf:
          - $ref: '../schemas/domain.yml'
          - type: object
            properties:
              items:
                $ref: '../schemas/item-statistics.yml'

