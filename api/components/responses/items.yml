type: array
items:
  allOf:
    - $ref: '../schemas/value-group.yml'
    - type: object
      required:
        - items
      properties:
        items:
          $ref: '../schemas/item-statistics.yml'
